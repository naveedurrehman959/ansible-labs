---
- name: installing package
  hosts: all
  become: true

  tasks:
  - name: installing nginx
    apt:
      name: nginx
      state: present
      update_cache: yes
  - name: starting service
    service:
      name: nginx 
      state: started
      enabled: true

